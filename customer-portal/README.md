# Customer Portal

Run:
```bash
cd customer-portal
npm install
npm run dev
```

Pages:
- `/upload-invoice` - validate invoice and redirect to `/create-ticket`
- `/create-ticket` - multi-step service order form
- `/check-ticket` - lookup ticket by ID
